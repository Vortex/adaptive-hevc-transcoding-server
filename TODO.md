# TODO - Adaptive HEVC Transcoding Server

## Pending

- [ ] Add streaming encode mode (stdin/stdout) to reduce temporary disk writes.
- [ ] Add optional per-chunk progress endpoint (SSE/WebSocket).
- [ ] Add queueing/scheduling for multiple worker nodes.

